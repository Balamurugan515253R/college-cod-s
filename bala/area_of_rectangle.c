#include<stdio.h>
int main()
{
   float a,l,b;
   printf("entre the length and breadth of the rectsngle");
   scanf("%f%f",&l,&b);
   a=l*b;
   printf("the area of the reactangle is %f",a);
   return 0;
}
